# Base URL of the NestJS API this app calls.
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Public origin this app is served from. Used for canonical URLs, Open Graph
# tags and the sitemap, which must all be absolute.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
